"""Fixtures compartilhadas entre `tests/unit` e `tests/integration`. Vazio por
enquanto — nenhum teste unitário toca banco/rede, e as fixtures específicas de
integração (skip automático, client HTTP) vivem em `tests/integration/conftest.py`.
"""
